{
  "version": 2,
  "buildCommand": "cd apps/web && ./node_modules/.bin/vite build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install --legacy-peer-deps && npm install --prefix apps/api --legacy-peer-deps && npm install --prefix apps/web --legacy-peer-deps --include=dev",
  "functions": {
    "api/handler.js": {
      "maxDuration": 30,
      "memory": 1024
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/handler.js" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
